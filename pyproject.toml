[project]
name = "fraud-transaction-monitor"
version = "2.0.0"
description = "Monitor experimental de fraude em transações com alertas explicáveis."
requires-python = ">=3.10"
dependencies = ["numpy>=1.26,<3", "pandas>=2.2,<4"]

[tool.ruff]
line-length = 120
target-version = "py310"
